ext {
    minSdkVersion = 24
    compileSdkVersion = 36
    targetSdkVersion = 36
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'

    // Kotlin — shared by ring alarm (Tier 2, ADR-0019) and the home-screen
    // widget (ADR-0020), first Kotlin usage in this module. Must match the
    // hardcoded classpath version in build.gradle's buildscript block.
    kotlinVersion = '1.9.24'
    // Home-screen widget (ADR-0020)
    androidxGlanceVersion = '1.1.1'
    androidxWorkVersion = '2.9.1'
}